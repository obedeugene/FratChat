==FratChat

FratChat is an app that allows Fraternities and Sororities to chat with each other.


    $ cd /tmp
    $ git clone git@github.com:obedeugene/fratchat.git
    $ cd fratchat
    $ bundle install
    $ rake db:migrate
    $ rake db:test:prepare

